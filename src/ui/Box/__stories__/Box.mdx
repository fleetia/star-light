import { Meta, Canvas, Controls, ArgTypes } from "@storybook/addon-docs/blocks";
import * as Stories from "./Box.stories";

<Meta of={Stories} />

# Box

콘텐츠를 감싸는 컨테이너 컴포넌트입니다. 선택적으로 제목을 표시할 수 있습니다.

## 사용법

<Canvas of={Stories.WithTitle} />
<Controls of={Stories.WithTitle} />

## 변형

### 제목 없음

<Canvas of={Stories.NoTitle} />

### 여러 자식 요소

<Canvas of={Stories.WithMultipleChildren} />

## Props

<ArgTypes of={Stories} />

## 접근성

- 제목이 있는 경우 `region` 역할과 `aria-labelledby`를 사용하여 섹션을 구분합니다.
- 내부 콘텐츠는 일반적인 문서 흐름을 따릅니다.
- 그룹화가 필요한 경우 `group` 역할을 적용할 수 있습니다.
