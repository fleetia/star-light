#!/bin/sh

CHANGED=$(git diff --name-only origin/main...HEAD 2>/dev/null || git diff --name-only HEAD~1)

# data/ 만 변경된 경우 스킵
if echo "$CHANGED" | grep -qv '^data/' 2>/dev/null; then
  :
else
  echo "data only — skipping tests"
  exit 0
fi

pnpm test
